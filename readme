To execute this console app you need to satisfy few dependency (java 8, git, maven),

    sudo apt-get update
    sudo add-apt-repository ppa:openjdk-r/ppa
    sudo apt-get update
    sudo apt-get install openjdk-8-jdk
    sudo apt-get install maven


    mvn clean package